const {people,ages} = require('./people');
// ./ represent look into the same directory

console.log(people, ages);

const os = require('os');
console.log(os);

console.log(os.platform(), os.homedir()); //info about os
